/**
 * AISettingsTab — embedded inside OrganizationSettingsPage.
 * Re-uses the full AISettingsPage layout directly.
 */
import { AISettingsPage } from "../pages/AISettingsPage"

export function AISettingsTab() {
    return <AISettingsPage />
}
